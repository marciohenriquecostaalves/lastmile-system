import { Order, OrderStatus, ComprovanteEntrega } from "./types"; const orders: Order[] = []; export function criarPedido(pedido: Order) { orders.push(pedido); return pedido; } export function listarPedidos() { return orders; } export function buscarPedido(id: string) { return orders.find((o) => o.id === id); } export function atualizarStatusPedido( id: string, status: OrderStatus, comprovante?: ComprovanteEntrega ) { const pedido = orders.find((o) => o.id === id); if (!pedido) return null; pedido.status = status; if (comprovante) pedido.comprovante = comprovante; return pedido; } 