services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: lastmile
      POSTGRES_PASSWORD: lastmile123
      POSTGRES_DB: lastmile
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
